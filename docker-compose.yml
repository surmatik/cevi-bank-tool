version: '3'

services:
  web:
    image: ghcr.io/surmatik/cevicity-bank:latest
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      FLASK_APP: app.py
      FLASK_ENV: production
